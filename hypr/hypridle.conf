# Minimal hypridle: only glue to lock the session (Noctalia's lock screen) on
# any suspend. All idle timers are handled natively by Noctalia (idle.behavior).
general {
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}
